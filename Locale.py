locations = ['forest', 'Cabin', 'Tundra', 'Ocean', 'Cave', 'Tunnel', 'House', 'Hospital']
track_loc = [False, False, False, False, False, False, False, False]
def get_int():
    choice =input(user)  # gets the user input and compares it to paramaters
    while choice not in result:
        choice = input("Invalid command, try again: ")

class Locale:


    def __init__(self, description_before, description_after, items, actions):
        self.description_before = description_before
        self.description_after = description_after
        self.items = items
        self.actions = actions

def run_location(self, description_before, description_after, items, actions):
    print(description_before)
    self.actions = choice = get_int("Please type an action: ",['forward', 'left', 'right', 'points', 'quit', 'help'], limit).lower()
    self.items = ""
    print(description_after)

def 